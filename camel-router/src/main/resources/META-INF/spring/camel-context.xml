<?xml version="1.0" encoding="UTF-8"?>
<!-- Configures the Camel Context -->
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd        http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd">

    <!-- ActiveMQ Connection -->
    <bean class="org.apache.activemq.camel.component.ActiveMQComponent" id="activemq">
        <property name="brokerURL" value="tcp://localhost:61616"/>
        <property name="userName" value="admin"/>
        <property name="password" value="admin"/>
    </bean>
    <!-- Here is a sample which processes the input files (leaving them in place 
		- see the 'noop' flag), and sends them on a JMS queue. This queue is watched 
		by another route which then performs content based routing on the messages 
		using XPath. -->
    <camelContext id="camel-router" trace="true" xmlns="http://camel.apache.org/schema/spring">
        <route id="consume-rnw">
            <from id="_from1" uri="activemq:OrderAckToRNW"/>
			<!-- Content Based Enrichment -->
			<enrich uri="file:src/data/enrich?noop=true"/>
			<to uri="http4://localhost:18080/RNW"/>
        </route>
        <route id="consume-atg">
            <from id="_from2" uri="activemq:OrderAckToATG"/>
			<!-- Transformation -->
			<to uri="http4://localhost:18080/ATG"/>
        </route>
        <route id="consume-payment">
            <from id="_from3" uri="activemq:OrderAckToPayment"/>
			<!-- Validation -->
			<to uri="http4://localhost:18080/Payment"/>
        </route>
    </camelContext>
</beans>
